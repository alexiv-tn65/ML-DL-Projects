# Makefile используется для автоматизации процесса сборки и тестирования проекта. 
# В вашем примере Makefile определяет несколько целей (targets), каждая из которых 
# представляет собой определенное действие, которое может быть выполнено с помощью команды make.



# Define targets
# .PHONY используется для указания фиктивных целей (phony targets)
# обычно используются для задач, которые не связаны с созданием файлов
.PHONY: pretty test

# Цель (target) pretty, использующая цели (dependencies) isort и black
# isort: запускает инструмент isort для автоматического форматирования импортов
# black: запускает инструмент black для автоматического форматирования кода в соответствии с PEP 8
pretty: isort black

# Цель (target) black, использующая инструмент black для автоматического форматирования кода
black:
	black . --exclude env/ # Форматирование всех файлов в текущей директории, исключая папку 'env'

# Цель (target) isort, использующая инструмент isort для автоматической сортировки импортов
isort:
	isort . --skip env/ # Сортировка импортов во всех файлах, пропуская папку 'env'

# Цель (target) test_ml, запускающая тесты для машинного обучения
test_ml:
	pytest tests/test_ml.py

# Цель (target) test_app, запускающая тесты для приложения
test_app:
	pytest tests/test_app.py
	